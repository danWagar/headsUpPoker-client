import React from 'react';

const LandingPage: React.FC = () => {
  return (
    <section>
      <h2>Take on opponents and rise through the ranks</h2>
    </section>
  );
};

export default LandingPage;
